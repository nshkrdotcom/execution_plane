# Used by "mix format"
[
  inputs: [
    "{mix,.formatter}.exs",
    "{config,lib,test}/**/*.{ex,exs}",
    "{core,protocols,streaming,placements,sandboxes,conformance}/*/{mix,.formatter}.exs",
    "{core,protocols,streaming,placements,sandboxes,conformance}/*/{lib,test}/**/*.{ex,exs}",
    "{core,protocols,streaming,placements,sandboxes,conformance}/*/.formatter.exs",
    "runtimes/*/{mix,.formatter}.exs",
    "runtimes/*/{lib,test}/**/*.{ex,exs}"
  ]
]
