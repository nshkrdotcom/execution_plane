# Installation

Add the package as a dependency:

```elixir
def deps do
  [
    {:execution_plane_websocket, "~> 0.1.0"}
  ]
end
```

Use a sibling path dependency while working in the workspace. Use the Hex
version when you publish the package.
