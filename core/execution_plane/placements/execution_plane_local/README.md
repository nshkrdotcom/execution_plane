# `placements/execution_plane_local`

Owns same-node placement semantics below family kits and above runtime launch.

Wave 2 status:

- active
- owns the narrow `execution_surface` contract vocabulary for the minimal slice
- placement remains intentionally narrow and transport-only
