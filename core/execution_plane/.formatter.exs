# Used by "mix format"
[
  inputs: [
    "{mix,.formatter}.exs",
    "{config,lib,test}/**/*.{ex,exs}",
    "{core,placements,conformance}/*/{mix,.formatter}.exs",
    "{core,placements,conformance}/*/{lib,test}/**/*.{ex,exs}",
    "{core,placements,conformance}/*/.formatter.exs"
  ]
]
